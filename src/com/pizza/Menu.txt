
Pizza Menu
#1. Vevulala tomat,Yikers        57 kr
#2. Laesdd tomat, POGGERS        54 kr
#3. Caccitore tomat, pepperoni   42 kr


final Scanner scanner = new Scanner(PizzaMenu);
while (scanner.hasNextLine()) {
   final String lineFromFile = scanner.nextLine();
   if(lineFromFile.contains(name)) {
       // a match!
       ArrayList<Something> liste = new ArrayList<>();
       break;
   }